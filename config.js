// Public deployment configuration. Set API_BASE_URL to the deployed Vercel URL (without a trailing slash).
// This file intentionally contains no credentials.
window.M1_CONFIG = window.M1_CONFIG || { API_BASE_URL: '' };
